#define toStr(s) #s
#define io(v) cin >> v
#define foreach(v, i) for(int i = 0; i < v.size(); i++)
#define FUNCTION(name, oper) inline void name(int &a, int b) {(a oper b)? a : a = b;}
#define INF 2147483647